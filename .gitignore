# Dependencies
node_modules/
server/node_modules/

# Build output (kept tracked so Render can serve it as static)
# dist/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local env
.env
.env.local
*.local

# Gradle / Android build (kept out of client repo)
android/.gradle/
android/build/
android/app/build/
android/local.properties

# Release signing keystore (NEVER commit)
android/keystore/
*.jks
*.keystore
